extends base

block content
    h1 🚨공사중🚨
    form
        h1 가져오기   
        label(for="catching_text") 데이터 가져오기
        input(id="catching_text" type="text" placeholder="가져온 텍스트가 여기에 표시됩니다")
        label(for="setpw") 패스워드
        input(type="password" value="***")
        input(type="submit" value="Catch!")
        small 클라이언트에서 위 패스워드를 입력하세요